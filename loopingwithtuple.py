#Notes - immutable lists, can be one item, a comma is needed (1,)

#you can loop over* all values in a tuple using a for loop

dimensions = (200, 50)
for dimension in dimensions:
    print(dimension)
#returns all elements in tuple, same with list (note this is not a generated list from a range function, so just to elements